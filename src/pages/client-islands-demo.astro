---
import Counter from '../components/Counter';
import Layout from '../layouts/Layout.astro';


// Static content is processed at build time
const pageTitle = "Client Islands Demo";
const currentDate = new Date().toLocaleDateString();

---

<Layout title={pageTitle}>
  <main class="container mx-auto p-4 max-w-4xl">
    <h1 class="text-4xl font-bold mb-2 text-center">{pageTitle}</h1>
    <p class="text-center text-gray-600 mb-8">Demonstrating Astro's component islands architecture</p>
    <!-- Client Island Section -->

      <div class="mb-6">
        <h2 class="text-2xl font-bold mb-2">Interactive Islands</h2>
        <p class="text-gray-700">
          Below is an Astro component that's initially rendered as static HTML, then "hydrated" on the client to become interactive.
        </p>
      </div>
      
      <!-- The client:load directive makes this an interactive island -->
      <Counter client:load />

    <section class="mt-12 text-center text-gray-600 border-t pt-6">
      <a href="/" class="text-blue-600 hover:underline mt-2 inline-block">Back to Home</a>
    </section>
  </main>
</Layout>